'use strict';

const createSchema = require('./create.schema');
const removeSchema = require('./remove.schema');
const listSchema = require('./list.schema');

module.exports = {createSchema, removeSchema, listSchema};
